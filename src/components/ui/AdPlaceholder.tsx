/**
 * Placeholder for future ad integration.
 * Returns null for launch — no visible ad slots until SDK is integrated.
 */
export default function AdPlaceholder({
}: {
  slot: "interstitial" | "post-result" | "banner";
}) {
  return null;
}
